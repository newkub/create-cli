/**
 * Infrastructure configuration
 * Clean Architecture 2 - Infrastructure layer
 */

// Re-export terminal configuration
export {
	createTerminalConfig,
	defaultTerminalConfig,
	type TerminalConfig,
} from "./terminal-config";
